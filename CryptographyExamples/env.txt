run:
macOS/Linux:
	python3 -m venv .venv     
Windows:
	python -m venv .venv
in terminal to create a new virtual environment.

Switch to the venv: Ctrl+Shift+` 

Then:
	pip install cryptography
...etc.



